{
  "name": "tracker",
  "version": "1.0.0",
  "description": "Node.js app with Express, JSON Server, and HTML pages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "json-server": "json-server --watch db.json --port 3001",
    "dev": "concurrently \"npm run start\" \"npm run json-server\""
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.4.0",
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
